// Placeholder slice for auth state

const initialState = {
  user: null,
  token: null,
}

export default function authSlice(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
