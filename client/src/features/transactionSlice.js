// Placeholder slice for transaction state

const initialState = {
  transactions: [],
}

export default function transactionSlice(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
