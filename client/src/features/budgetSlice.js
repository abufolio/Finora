// Placeholder slice for budget state

const initialState = {
  budgets: [],
}

export default function budgetSlice(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
